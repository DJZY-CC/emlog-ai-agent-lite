[
  {
    "name": "get_categories",
    "description": "获取博客全站分类列表（ID和名称）",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "get_tags",
    "description": "获取博客已有标签列表，支持关键词搜索",
    "parameters": {
      "type": "object",
      "properties": {
        "keyword": {
          "type": "string",
          "description": "搜索关键词（可选）"
        }
      },
      "required": []
    }
  },
  {
    "name": "search_articles",
    "description": "全文搜索博客文章",
    "parameters": {
      "type": "object",
      "properties": {
        "keyword": {
          "type": "string",
          "description": "搜索关键词"
        },
        "page": {
          "type": "integer",
          "description": "页码，默认1"
        },
        "limit": {
          "type": "integer",
          "description": "每页数量，默认10"
        }
      },
      "required": [
        "keyword"
      ]
    }
  },
  {
    "name": "get_articles",
    "description": "按分类或排序方式获取文章列表",
    "parameters": {
      "type": "object",
      "properties": {
        "sortid": {
          "type": "integer",
          "description": "分类ID（可选）"
        },
        "order": {
          "type": "string",
          "description": "排序方式：new/old/hot（默认new）"
        },
        "limit": {
          "type": "integer",
          "description": "数量限制，默认10"
        },
        "page": {
          "type": "integer",
          "description": "页码，默认1"
        }
      },
      "required": []
    }
  },
  {
    "name": "get_article",
    "description": "获取单篇文章的完整内容",
    "parameters": {
      "type": "object",
      "properties": {
        "aid": {
          "type": "integer",
          "description": "文章ID"
        }
      },
      "required": [
        "aid"
      ]
    }
  },
  {
    "name": "get_stats",
    "description": "获取博客站点统计数据（文章数、分类数、评论数等）",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "get_comments",
    "description": "获取评论列表，支持按状态筛选",
    "parameters": {
      "type": "object",
      "properties": {
        "limit": {
          "type": "integer",
          "description": "返回数量，默认10"
        },
        "status": {
          "type": "string",
          "description": "状态筛选：all/pending/approved（默认all）"
        }
      },
      "required": []
    }
  }
]